# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
ENV/
.env
# Local copy of voice settings (template: voice_config.example.yaml)
voice_config.yaml
*.egg-info/
.eggs/
dist/
build/

# Testing / coverage
.pytest_cache/
.coverage
htmlcov/

# Node (React demo) — never commit; run `npm install` in frontend-react/ after clone
node_modules/
frontend-react/dist/
frontend-react/.env
frontend-react/.env.local

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Secrets (never commit)
*.pem
*API_KEY*.txt

# Legacy / experimental dataset trees (production = data_manifest_update_hy/)
data/
data_manifest_update_multi/
data_validation/
data_ameriabank_fix/

# Scraped HTML cache (reproducible via `scrape`; chunks + index are enough for eval)
data_manifest_update_hy/raw_html/

# FAISS build outputs (regenerate with `build-index`; see index/hy_model_index/README.md)
data_manifest_update_hy/index/**/faiss.index
data_manifest_update_hy/index/**/metadata.jsonl
data_manifest_update_hy/index/**/index_info.json

# Local / editor junk
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
